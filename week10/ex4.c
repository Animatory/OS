#include <stdlib.h>
#include <stdio.h>
#include <time.h>

//int main(){
//    dirp = opendir(".");
//    if (dirp == NULL) { return (ERROR); }
//    len = strlen(name);
//    while ((dp = readdir(dirp)) != NULL) {
//        if (dp->d_namlen == len &&
//            strcmp(dp->d_name, name) == 0) {
//            (void)closedir(dirp);
//            return (FOUND);
//        }
//    }
//    (void)closedir(dirp);
//    return (NOT_FOUND);
//}

